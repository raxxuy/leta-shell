{
  "scripts": {
    "lint": "biome check",
    "build": "rm result -r; nix build",
    "start": "./result/bin/leta-shell",
    "clear": "rm $HOME/.cache/leta-shell -r",
    "dev": "./watch.sh"
  },
  "dependencies": {
    "@biomejs/biome": "^2.3.3",
    "ags": "*",
    "gnim": "*"
  },
  "prettier": {
    "semi": true
  }
}
